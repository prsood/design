cmake_minimum_required(VERSION 3.6)
project(shell)
set(CTRL udp-client.c)
set(REVERSE reverse_udp.c)
#ADD_LIBRARY(pthread)
add_executable(ctrl ${CTRL})
target_link_libraries(ctrl pthread)
add_executable(client ${REVERSE})
target_link_libraries(client pthread)
